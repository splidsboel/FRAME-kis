# Generated artifacts — regenerate via queryset/build.py + oracle/build_gt.py
data/*.jsonl
# probe dumps (author_probe.json, *.unions.json)
data/*.json
# canonical Tier-2 dataset — multi-GB parquet/h5, rebuilt by scripts/prep_*.py
data/canonical/
# corpus characterization dumps (scripts/data_stats.py)
data/*.csv
# scripts/author_probe.py --unions input (one-off, regenerable)
candidates*.json

# SLURM job output — routed to logs/ by every *.sh (logs/.gitkeep is tracked)
*.out
slurm-*.out

# Figures — regenerated from the data artifacts by scripts/plot_*.py, and copied
# into the thesis LaTeX project when a version is actually used. Kept out of git
# so re-runs don't churn binaries (results/figures/.gitkeep is tracked).
results/*
!results/figures/
results/figures/*
!results/figures/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# uv / venv  (uv.lock IS committed — reproducible installs)
.venv/

# editor / OS
.DS_Store
.vscode/
.idea/
